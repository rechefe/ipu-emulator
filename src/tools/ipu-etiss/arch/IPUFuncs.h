/* Public surface of the IPU semantics library.
 *
 * The prototypes themselves are generated from INSTRUCTION_SPEC; this header
 * only pulls them in together with the CPU state definition, so both the
 * plugin and the JIT-compiled blocks see one consistent view.
 */
#ifndef IPU_FUNCS_H
#define IPU_FUNCS_H

#include "IPU/IPU_gen.h"
#include "IPU/IPUFuncs_gen.h"

#endif /* IPU_FUNCS_H */
